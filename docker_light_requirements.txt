# Core
numpy==1.26.4
# torch==2.3.1+cpu  # installed separately via --index-url
# torchvision==0.18.1+cpu  # installed separately via --index-url

# File I/O
nibabel==5.3.2
SimpleITK==2.4.0

# Image processing
scikit-image==0.24.0
scipy  # for ndimage (no need to pin, comes with scikit-image)
scikit-spatial==8.0.0

# Config / CLI
hydra-core==1.3.2
hydra-colorlog==1.2.0

# Viz (only if cfg.show_result_gifs is ever True in Docker — probably not)
matplotlib==3.9.2

# Other
tqdm==4.67.1
